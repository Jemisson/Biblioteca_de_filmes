<h1><%= t('views.movies.edit.title') %></h1>

<%= render "form", movie: @movie %>

<br>

<div>
  <%= link_to t('views.movies.show.title'), @movie %> |
  <%= link_to t('views.movies.index.title'), movies_path %>
</div>
